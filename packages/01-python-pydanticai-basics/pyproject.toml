[project]
name = "pydanticai-basics"
version = "1.0.0"
description = "阶段零（Python 线）：Pydantic AI 基础机制，对照 LangGraph basics"
requires-python = ">=3.11"
dependencies = [
    "pydantic-ai>=1.105.0",
    "python-dotenv>=1.2.2",
]

[dependency-groups]
dev = [
    "mypy>=2.1.0",
]

[tool.uv]
package = false
